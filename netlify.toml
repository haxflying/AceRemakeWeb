# ACE REMAKE static site
# The auth service is deployed separately; update index.html ace-auth-api-base
# or place both behind the same origin proxy before production.
[build]
  publish = "."
  command = ""
